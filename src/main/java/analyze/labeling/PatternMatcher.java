package analyze.labeling;

import java.util.ArrayList;

import model.SequentialData;

/**
 * This class represents a pattern matcher on the sequential data.
 * @author Hans Schouten
 *
 */
public class PatternMatcher {

    /**
     * Find all occurrences of the given pattern.
     * @param pattern
     * @return
     */
    public ArrayList<SequentialData> match(String pattern, SequentialData data) {
        if(pattern.contains("PATTERN(")) {
            
        }
        return null;
    }

}
